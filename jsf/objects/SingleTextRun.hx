package jsf.objects;
import jsf.objects.TextAttrs;

typedef SingleTextRun={
	var changedAttrs:TextAttrs; /* TextAttrs object (see TextAttrs object).*/ 
	var characters:String; /* The text that is contained in this run.*/ 
}